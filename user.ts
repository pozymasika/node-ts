import type { User } from "./types.ts";
// import { Roles } from "./types.ts";

const user: User = {
  id: 1,
  // role: Roles.Admin,
  name: "John Doe",
  email: "john@example.com",
};

export default user;
